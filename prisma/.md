generator client {
provider = "prisma-client-js"
previewFeatures = ["postgresqlExtensions"]
}

datasource db {
provider = "postgresql"
url = env("DATABASE_URL")
extensions = [ pgcrypto]
}

enum AccountStatus {
active
dormant
suspended
}

enum Gender {
male
female
others
}

enum ReservationType {
booked
reserve
}

enum PaymentStatus {
paid
pending
cancel
}

enum PaymentMethod {
cash
creditCard
khqr
leave
}

enum Status {
active
inactive
}

model State {
stateId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
name String @unique

cities City[]
employees Employeeinfo[]
customers Customer[]
}

model City {
cityId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
stateId String @db.Uuid
name String

@@unique([name, stateId])
state State @relation(fields: [stateId], references: [stateId])
employees Employeeinfo[]
customers Customer[]
}

model Token {
tokenId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
authId String? @db.Uuid
userId String? @db.Uuid
token String @unique
deviceInfo String @default("")
ipAddress String @default("")
expiresAt DateTime
createdAt DateTime @default(now())

auth Auth? @relation(fields: [authId], references: [authId])
user User? @relation(fields: [userId], references: [userId])

@@index([token])
@@index([expiresAt])
}

model Role {
roleId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
name String @unique
status Status @default(active)

auths Auth[]
}

model Auth {
authId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
email String @unique @db.VarChar(255)
password String @db.VarChar(255)
roleId String @db.Uuid
employeeId String? @db.Uuid @unique
status AccountStatus @default(active)
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

role Role? @relation(fields: [roleId], references: [roleId])
token Token[]
employee Employee? @relation(fields: [employeeId], references: [employeeId])
cart Cart[]
logs AuthLog[]
groupchat Groupmessage[]
notification Notification[]
shift Shift[]

@@index([email])
}

model User {
userId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
email String @unique @db.VarChar(255)
password String @db.VarChar(255)
customerId String? @db.Uuid @unique
status AccountStatus @default(active)
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

token Token[]
customer Customer? @relation(fields: [customerId], references: [customerId])
cart Cart[]
notification Notification[]

@@index([email])
}

model Attendance {
attendanceId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
employeeId String @db.Uuid
datetime DateTime @default(now())
note String?
status Status @default(active)

employee Employee @relation(fields: [employeeId], references: [employeeId])
}

model Employee {
employeeId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
employeeCode String? @unique
firstName String
lastName String
gender Gender @default(male)
dob DateTime?
phone String? @unique
positionId String @db.Uuid  
 departmentId String @db.Uuid  
 salary Decimal @db.Decimal(10,2)
hiredDate DateTime?
status Status @default(active)
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

auth Auth?
info Employeeinfo?
position Position? @relation(fields: [positionId], references: [positionId])
department Department? @relation(fields: [departmentId], references: [departmentId])
reservedetails Reservedetail[]
sales Sale[]
attendances Attendance[]
payment Payment[]
}

model Employeeinfo {
employeeinfoId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
employeeId String @db.Uuid @unique
picture String?
region String?
email String? @unique
address String?
cityId String @db.Uuid
stateId String @db.Uuid
country String?
note String?
status Status @default(active)

employee Employee @relation(fields: [employeeId], references: [employeeId])
state State @relation(fields: [stateId], references: [stateId])
city City @relation(fields: [cityId], references: [cityId])
}

model Department {
departmentId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
departmentName String
departmentCode String?
memo String?
status Status @default(active)
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

positions Position[]
employees Employee[]
}

model Position {
positionId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
departmentId String @db.Uuid
positionName String?
positionCode String?
memo String?
status Status @default(active)
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

department Department @relation(fields: [departmentId], references: [departmentId])
employees Employee[]
}

model Customer {
customerId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
firstName String
lastName String
picture String?
gender Gender @default(male)
email String? @unique
phone String?
address String?
cityId String @db.Uuid
stateId String @db.Uuid
status Status @default(active)
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

state State @relation(fields: [stateId], references: [stateId])
city City @relation(fields: [cityId], references: [cityId])
user User?
sales Sale[]
reservedetails Reservedetail[]
}

model Reservedetail {
reservedetailId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
roomId String @db.Uuid
employeeId String @db.Uuid
customerId String @db.Uuid @unique
reservationId String @db.Uuid
price Decimal @db.Decimal(10,2)  
 night Int
amount Decimal @db.Decimal(10,2)
status Status @default(active)

room Room @relation(fields: [roomId], references: [roomId])
employee Employee @relation(fields: [employeeId], references: [employeeId])
customer Customer? @relation(fields: [customerId], references: [customerId])
reservation Reservation @relation(fields: [reservationId], references: [reservationId])
}

model Reservation {
reservationId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
checkinDate DateTime
checkoutDate DateTime
isCheckin Boolean
isCheckout Boolean
reservationType ReservationType @default(reserve)
adults Int @default(1)
children Int @default(0)
paymentStatus PaymentStatus @default(paid)
paymentMethod PaymentMethod @default(cash)
memo String?
status Status @default(active)
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

details Reservedetail[]
payment Payment[]
}

model Room {
roomId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
typeId String @db.Uuid  
 roomName String?
price Decimal @db.Decimal(10,2)
isAc Boolean @default(true)
capacity Int @default(4)
size Int @default(25)
discountRate Int @default(0)
isBooked Boolean @default(false)
status Status @default(active)
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

roomtype Roomtype? @relation(fields: [typeId], references: [typeId])
pictures Roompicture[]
reservedetails Reservedetail[]
sales Sale[]
}

model Roomtype {
typeId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
name String @unique
code String @unique
status Status @default(active)

rooms Room[]
}

model Roompicture {
roompictureId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
roomId String @db.Uuid
picture String
status Status @default(active)
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

room Room @relation(fields: [roomId], references: [roomId])
}

model Product {
productId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
productName String
productCode String?  
 categoryId String @db.Uuid
picture String?  
 price Decimal @db.Decimal(10,2) @default(0.00)
discountRate Int @default(0)
status Status @default(active)
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

category Category? @relation(fields: [categoryId], references: [categoryId])
stocks Productstock[]
saledetails Saledetail[]
cart Cart[]
}

model Category {
categoryId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
picture String?  
 categoryName String
categoryCode String?
memo String?  
 status Status @default(active)
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

products Product[]
}

model Cart {
cartId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
authId String? @db.Uuid
userId String? @db.Uuid
productId String @db.Uuid
quantity Int
status Status @default(active)
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

product Product @relation(fields: [productId], references: [productId])
auth Auth? @relation(fields: [authId], references: [authId])
user User? @relation(fields: [userId], references: [userId])
note Cartnote[]
}

model Cartnote {
cartnoteId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
cartId String @db.Uuid
note String
status Status @default(active)

cart Cart @relation(fields: [cartId], references: [cartId])
}

model Productstock {
productstockId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
productId String @db.Uuid
supplierId String @db.Uuid
invNumber String?
productAdd Int @default(0)
addPrice Decimal @db.Decimal(12,2) @default(0.00)
addDate DateTime?
memo String?
status Status @default(active)
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

product Product? @relation(fields: [productId], references: [productId])
supplier Supplier? @relation(fields: [supplierId], references: [supplierId])
}

model Supplier {
supplierId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
supplierName String
companyName String?
phone String?
email String?
address String?
status Status @default(active)
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

stocks Productstock[]
}

model Sale {
saleId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
employeeId String @db.Uuid
roomId String @db.Uuid
customerId String @db.Uuid
saleDate DateTime @default(now())
amount Decimal @db.Decimal(12,2)
status Status @default(active)
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

room Room? @relation(fields: [roomId], references: [roomId])
employee Employee @relation(fields: [employeeId], references: [employeeId])
customer Customer? @relation(fields: [customerId], references: [customerId])
saledetails Saledetail[]
payment Payment[]
}

model Saledetail {
saledetailId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
saleId String @db.Uuid
productId String @db.Uuid
quantity Int
amount Decimal @db.Decimal(12,2)
status Status @default(active)
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

sale Sale @relation(fields: [saleId], references: [saleId])
product Product @relation(fields: [productId], references: [productId])
}

model Payment {
paymentId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
employeeId String @db.Uuid
saleId String @db.Uuid
reservationId String @db.Uuid
invoice String?
hash String
fromAccountId String
toAccountId String
currency String
amount Decimal @db.Decimal(12,2)
externalRef String
status Status @default(active)
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

sale Sale? @relation(fields: [saleId], references: [saleId])
reservation Reservation? @relation(fields: [reservationId], references: [reservationId])
employee Employee @relation(fields: [employeeId], references: [employeeId])
}

model Shift {
shiftId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
authId String @db.Uuid
openKhmerRiel Decimal @db.Decimal(12,2) @default(0.00)
openUsDollar Decimal @db.Decimal(12,2) @default(0.00)
openTime DateTime @default(now())
closeKhmerRiel Decimal? @db.Decimal(12,2) @default(0.00)
closeUsDollar Decimal? @db.Decimal(12,2) @default(0.00)
closeTime DateTime?  
 status Status @default(active)
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

banknotes Banknote[]
auth Auth? @relation(fields: [authId], references: [authId])
}

model Banknote {
banknoteId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
shiftId String @db.Uuid
khmer100 Int? @default(0)
khmer500 Int? @default(0)
khmer1K Int? @default(0)
khmer2K Int? @default(0)
khmer5K Int? @default(0)
khmer10K Int? @default(0)
khmer15K Int? @default(0)
khmer20K Int? @default(0)
khmer30K Int? @default(0)
khmer50K Int? @default(0)
khmer100K Int? @default(0)
khmer200K Int? @default(0)
us1 Int? @default(0)
us5 Int? @default(0)
us10 Int? @default(0)
us20 Int? @default(0)
us50 Int? @default(0)
us100 Int? @default(0)
status Status @default(active)
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

shift Shift @relation(fields: [shiftId], references: [shiftId])
}

model Groupmessage {
groupmessageId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
authId String? @db.Uuid
content String
time DateTime
status Status @default(active)
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

auth Auth? @relation(fields: [authId], references: [authId])
}

model Notification {
notificationId String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
authId String? @db.Uuid
userId String? @db.Uuid
title String
content String
status Status @default(active)
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

auth Auth? @relation(fields: [authId], references: [authId])
user User? @relation(fields: [userId], references: [userId])
}

model Log {
id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
method String
url String
status Int
responseTime Float
ip String
userAgent String?
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt
}

model AuthLog {
id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
authId String @db.Uuid
method String
url String
status Int
responseTime Float
ip String
userAgent String?
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

auth Auth @relation(fields: [authId], references: [authId])
}

// ! test model
model Image {
id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
title String
imageUrl String
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt
}
