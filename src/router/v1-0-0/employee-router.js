// const express = require("express");
// const router = express.Router();
// const employeeController = require("@controllers/v1-0-0/human-resource/employee-controller");

// router.get("/:id?", employeeController.select.bind(employeeController));
// router.post("/", employeeController.create.bind(employeeController));
// router.put("/:id", employeeController.update.bind(employeeController));
// router.patch("/:id", employeeController.patch.bind(employeeController));
// router.delete("/:id", employeeController.destroy.bind(employeeController));

// module.exports = router;
